<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Pokemons Favoris</title>

    <style>
        * {
            box-sizing: border-box;
            font-size: 20px;
        }
        
        body {
            background: #0c1c3e;
        }
        
        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            width: 60%;
            margin: 50px auto;
            color: #fbfcfc;
        }
        
        .btn {
            margin: 10px;
            font-size: 15px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>
            <%= title %>
        </h1>
        <ul>
            <% favPokes.forEach((poke) => { %>
                <li id=<%=poke.name%> ><a href=<%=`/pokemon/${poke.name}`%>> <%= poke.name %> </a><button class="btn btn-danger" onclick="remove(`<%=poke.name%>`)">supprimer</button></li>
                <% }) %>
        </ul>
    </div>



    <script type="application/javascript">
        async function remove(name) {
            await fetch(`http://localhost:3000/pokemon/${name}`, {
                method: "DELETE",
                body: JSON.stringify({
                    name
                })
            })
            document.getElementById(name).remove()
        }
    </script>
</body>

</html>